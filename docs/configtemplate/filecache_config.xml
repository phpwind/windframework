<?xml version="1.0" encoding="UTF-8"?>
<!-- 文件缓存配置 -->
<cache>
	<!-- 缓存文件的保存路径（支持命名空间的方式配置该路径） -->
	<cache-dir value='WEB:compile' />
	<!-- 缓存的级别 -->
	<cache-level value='0' />
	<!-- 缓存文件的后缀 -->
	<cache-suffix value='php' />
	<!-- 缓存文件的key值是否经过安全处理 -->
	<security value='true' />
	<!-- 缓存文件的过期时间 -->
	<expires value='20' />
</cache>
